- 	If you pipe, the program will end after the piped input file has put everything in. Be sure to included all piped commands in the input file. 
	You'll see a bunch of "EnterCommands" because the piped commands are invisible, this isn't an error.

-	When adding a program, you must either include three or five arguments, any other will throw an error.

-	Quit instead of quit.